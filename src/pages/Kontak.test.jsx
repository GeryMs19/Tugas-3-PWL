import { render, screen } from "@testing-library/react";
import Kontak from "./Kontak";

describe("Kontak", () => {
  test("renders the Kontak component", () => {
    expect(true).toBe(true);
  });
});
